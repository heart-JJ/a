[project]
name = "evoagent"
version = "0.1.0"
description = "A local, auditable and reversible adaptive skill runtime"
requires-python = ">=3.11"

[project.scripts]
evoagent = "evoagent.__main__:main"

[tool.unittest]
start-directory = "tests"

